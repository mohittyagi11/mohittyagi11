package com.azadishashn.app.ui.theme

import androidx.compose.ui.unit.dp

/** Generous, consistent spacing scale — the app should feel airy, not cramped. */
object Dim {
    val screenH = 24.dp      // screen horizontal padding
    val screenV = 16.dp      // screen vertical padding
    val sectionGap = 20.dp   // gap between major sections
    val cardPad = 20.dp      // SectionCard inner padding
    val itemGap = 12.dp      // gap between list items
    val heroGap = 20.dp      // around hero headers
    val tight = 8.dp
}
